export default class Data {
    constructor(x, y, radius, level, delayTime) {
        this.x = x;
        this.y = y;        
        this.radius = radius;
        this.level = level;
        this.delayTime = delayTime;
    }
}